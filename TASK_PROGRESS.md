# TASK_PROGRESS

- 任务目标：将产品快速收敛为「总览 / 申请管理 / 任务执行面板 / AI 助手 / 设置」，并打通“输入 -> 草稿 -> 写入 -> 今日推进”闭环。
- 当前阶段：P0 进行中（本轮已完成裁切修复 + 任务执行面板 + AI 执行工作台重构）。
- 已完成：
  - 全局裁切修复：
    - `PanelCard` 默认不再 `overflow-hidden`，改为可配置裁切。
    - `PageShell` 去除内容区默认 `overflow-hidden`，避免正文卡片吞字。
  - 任务页重构为统一执行面板（替代旧的 today/backlog 双页逻辑）：
    - 顶部紧凑分桶摘要：`today / this_week / unscheduled / completed / at_risk`。
    - 主区：`今日日程` + `今日任务（必做/可推进）`。
    - 次级区：`mini calendar` 日期导航 + 当前分桶执行清单。
    - 右侧：风险列表 + 当前任务一键动作（今天/明天/本周/待安排/完成/删除）。
    - 保留快速写入、快速安排、快速改期、快速完成闭环。
  - AI 页重构为“执行工作台四区”：
    - 快捷动作：`生成今日计划 / 大任务拆解 / 写入任务日程 / 申请推进清单`。
    - 输入区：支持一句话与外部 prompt 粘贴。
    - 结构化草稿预览：先预览后确认写入。
    - 最近执行结果与草稿队列：可追踪回执、可选择草稿应用。
    - 流程固定：解析 -> 草稿预览 -> 保存草稿 -> 确认写入。
  - AI 自动化增强（懒人模式）：
    - 新增“懒人模式”开关：生成草稿后可自动保存并自动写入任务系统。
    - 新增“一键保存并写入”按钮，减少手动步骤。
  - AI 内部分析增强：
    - 新增“内部AI分析任务”快捷动作：直接从任务池计算优先级、DDL压力、未安排状态，自动生成 today/this_week/unscheduled 草稿。
    - 外部 prompt 解析保留为补充入口，内部优先、外部兜底。
  - AI 懒人入口收口：
    - 新增“`一句话直写入（推荐）`”按钮：输入一句话后直接解析并自动写入任务系统。
    - 新增快捷键 `Cmd/Ctrl + Enter` 触发直写入，进一步减少点击路径。
  - 执行面板分析增强：
    - 任务右栏新增“日程分析快照”（今日日程/今日必做/待安排/风险项），便于 AI 与人工快速判断负荷。
  - 日程模块重制（二轮）：
    - 今日日程改为“上午 / 下午 / 晚上”三段可视化，不再只是一条任务列表。
    - 新增“待安排池”并支持一键安排到早/午/晚时段。
    - 任务详情新增快速排程按钮（上午/下午/晚上），实现最少操作完成排程。
- 正在进行：
  - P0 收口检查：总览和申请管理页面继续去除“同权重白卡”并统一主次层级。
  - P1 预备：晨报/晚报精简模板与学校官网更新订阅推送联动。
- 待完成：
  - 晨报/晚报按 ADHD 短扫读模板落地。
  - 官网订阅推送策略（高即时/中汇总/低记录）与任务联动验证。
  - 任务/AI 对“外部 prompt 批量解析”补充更多申请语义规则。
- 改动文件：
  - `/Users/hujunbo/Documents/Playground/src/components/workspace/panel-card.tsx`
  - `/Users/hujunbo/Documents/Playground/src/components/layout/page-shell.tsx`
  - `/Users/hujunbo/Documents/Playground/src/pages/tasks/page.tsx`
  - `/Users/hujunbo/Documents/Playground/src/pages/ai/page.tsx`
- 已运行测试及结果：
  - `npm run build`：通过。
  - `cargo check --manifest-path src-tauri/Cargo.toml`：通过（存在既有 warning，无新增阻塞）。
- 当前阻塞：
  - 无功能阻塞；当前为体验收口阶段。
- 下一步：
  - 继续 P0：总览/申请管理做主次收紧与空态瘦身。
  - 紧接 P1：实现晨报/晚报固定模板 + 官网更新到任务的一键落地。
